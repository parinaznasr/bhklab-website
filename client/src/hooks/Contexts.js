import { createContext } from 'react';

export const AuthContext = createContext(null);
export const PipelineBuilderContext = createContext(null);
export const PipelineEditorContext = createContext(null);